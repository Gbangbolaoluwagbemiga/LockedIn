{
  "name": "lockedin",
  "version": "1.0.0",
  "description": "A decentralized commitment contract platform - Lock in your goals by staking ETH",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:alfajores": "hardhat run scripts/deploy.js --network alfajores",
    "deploy:celo": "hardhat run scripts/deploy.js --network celo",
    "verify:celo": "hardhat verify --network celo",
    "node": "hardhat node"
  },
  "keywords": [
    "solidity",
    "ethereum",
    "blockchain",
    "commitment",
    "staking",
    "defi"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

